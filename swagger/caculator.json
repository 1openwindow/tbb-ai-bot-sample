{
  "openapi": "3.0.0",
  "info": {
    "title": "Calculator API",
    "version": "1.0.0"
  },
  "paths": {
    "/caculator": {
      "get": {
        "summary": "Performs a calculation on two numbers",
        "description": "Calculates the result of adding two numbers together and then multiplying the sum by three.",
        "parameters": [
          {
            "name": "firstNumber",
            "in": "query",
            "required": true,
            "schema": {
              "type": "number"
            }
          },
          {
            "name": "secondNumber",
            "in": "query",
            "required": true,
            "schema": {
              "type": "number"
            }
          }
        ]
      }
    }
  },
  "components": {
    "securitySchemes": {
      "ApiKeyAuth": {
        "type": "apiKey",
        "in": "header",
        "name": "X-API-KEY"
      }
    }
  },
  "security": [
    {
      "ApiKeyAuth": []
    }
  ]
}